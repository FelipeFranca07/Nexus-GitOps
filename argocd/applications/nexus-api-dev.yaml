apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexus-api-dev
  namespace: argocd
spec:
  project: nexus
  source:
    repoURL: "https://github.com/FelipeFranca07/Nexus-GitOps.git"
    targetRevision: main
    path: apps/nexus-api/overlays/dev
  destination:
    server: "https://kubernetes.default.svc"
    namespace: nexus-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
